grumphp:
    tasks: 
        phpcs:
            standard: .phpcs.xml.dist
        phpstan:
            configuration: phpstan.neon.dist
        phpmd:
            whitelist_patterns:
                - 'src'
            ruleset:
                - 'phpmd.xml'
        phpunit:
            config_file: phpunit.xml